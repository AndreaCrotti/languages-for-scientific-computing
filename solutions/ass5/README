* Compiling
  Edit the Makefile and change those two variables:
  BLAS = $(HOME)/libs/c/GotoBLAS
  CC = gcc

  and then
  make will compile everything

* Known bugs
  - trsv.m doesn't work for b > 1
  - strange errors given by blas trsv, probably due to norm function or pointer error
